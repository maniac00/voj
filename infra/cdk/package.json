{
  "name": "voj-infra-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --require-approval never"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.154.0",
    "constructs": "^10.3.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2"
  }
}

