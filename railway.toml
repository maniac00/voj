[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
watchPatterns = ["backend/app/**", "backend/alembic/**"]

[deploy]
# Dockerfile CMD를 사용하므로 startCommand 제거
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
